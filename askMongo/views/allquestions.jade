extends layout

block content
 div#questions
    ul
     each val in questions
      li <b>#{val.name}</b> </br> #{val.question} <a href="addVote" class="votes">#{val.votes}</a>

block append scripts
 script(src='http://localhost:3000/js/allquestions.js')